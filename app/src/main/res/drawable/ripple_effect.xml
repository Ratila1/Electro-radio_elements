<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/light_blue_1"> <!-- Цвет ripple-эффекта (светло-голубой) -->

    <!-- Маска для ripple-эффекта -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/light_blue_1" /> <!-- Цвет маски -->
            <corners android:radius="12dp" /> <!-- Закругленные углы -->
        </shape>
    </item>

    <!-- Фон кнопки с градиентом -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/light_blue_1"
                android:endColor="@color/light_blue_2"
                android:angle="45"
                android:type="linear" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</ripple>